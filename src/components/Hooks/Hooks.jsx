import React from "react";
import { useState } from "react";
function Hooks() {
  return (
    <>
      <div className="border border-2 p-2">Hooks Example</div>
    </>
  );
}
export default Hooks;

// In older react version, we used class based component.
/* now class based component are not used so acheiving state, life cycle and other feature in
function component we used hooks. */
// functional component are easy to use and lightweight.
// useState, useEffect, useRef, useContext, useReducer etc.
